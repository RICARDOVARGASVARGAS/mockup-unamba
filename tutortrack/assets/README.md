# Assets — TutorTrack

Imágenes del mockup. Origen: carpeta `assets/` del workspace (y
retratos de ejemplo ya internos para avatares).

```
img/
  campus/     banner_1–3.png  ← PORTADA (1)–(3) del workspace
  facultad/   logos UNAMBA / Facultad de Administración
  docentes/   avatares de ejemplo (placeholders)
  estudiantes/ avatares de ejemplo (placeholders)
```

No referenciar URLs externas ni assets de `pagina-web/` o `docentrack/`.
